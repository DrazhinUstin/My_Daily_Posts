import { UpdateProfileForm, UpdateEmailForm } from '../components';

const Profile = () => {
    return (
        <main className='main'>
            <UpdateProfileForm />
            <UpdateEmailForm />
        </main>
    );
};

export default Profile;
